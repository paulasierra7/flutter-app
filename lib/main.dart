import 'package:flutter/material.dart'

void main(){

}

class MyApp extends StatelessWidget {
  Widget build(BuildContext context){
    return MaterialApp(home: Text(),);
    //BuildContext:
    //When defining a function, you can declare your args like this:

      // printElement(int element) {
      //   print(element);
      // }
      // var list = [1, 2, 3];
      // list.forEach(printElement);
      // When calling it, you pass through named params:

      // enableFlags(bold: true, hidden: false) {}estamos pasando paramentros 
  }
}